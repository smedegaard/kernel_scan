[project]
name = "kernel-scan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["numpy>=2.3.0", "polars>=1.30.0"]

[project.optional-dependencies]
visualization = ["jupyter>=1.1.1", "pandas>=2.3.0", "plotly>=6.1.2"]
